{
  "name": "boatmatey-monorepo",
  "private": true,
  "description": "Root package for BoatMatey. Develop in web/, then sync to android/ and ios/.",
  "scripts": {
    "build": "cd web && npm run build",
    "cap:android": "npm run build && npx cap copy android",
    "cap:ios": "npm run build && npx cap copy ios",
    "sync:android": "npx cap copy android",
    "sync:ios": "npx cap copy ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.1",
    "@capacitor/android": "^8.0.1",
    "@capacitor/ios": "^8.0.1"
  }
}
