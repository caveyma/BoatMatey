{
  "name": "boatmatey-monorepo",
  "private": true,
  "description": "Root package for BoatMatey. Develop in web/, then sync to android/ and ios/.",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "build:cloudflare": "cd web && npm ci && npm run build",
    "deploy:cloudflare": "npm run build:cloudflare && npx wrangler pages deploy web/dist --project-name=boatmatey",
    "cap:android": "npm run build && npx cap sync android",
    "cap:ios": "npm run build && npx cap sync ios",
    "sync:android": "npx cap sync android",
    "sync:ios": "npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/android": "^8.0.1",
    "@capacitor/cli": "^8.0.1",
    "@capacitor/ios": "^8.0.1",
    "wrangler": "^4.61.1"
  },
  "dependencies": {
    "@capacitor/camera": "^8.0.0",
    "@capacitor/core": "^8.0.1",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.1",
    "@revenuecat/purchases-capacitor": "^12.0.5",
    "jsonwebtoken": "^9.0.3"
  }
}
