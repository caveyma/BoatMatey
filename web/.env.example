# Copy to .env.local and fill in from Supabase Dashboard → Project Settings → API
# The anon key is a long JWT string starting with "eyJ..." (not a publishable key from another service).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# RevenueCat API Keys (from RevenueCat Dashboard)
# Android key is hardcoded in services/revenuecat.js
# iOS key should be set here:
VITE_REVENUECAT_API_KEY_IOS=appl_your_ios_api_key_here
